version: '3.8'

services:
  bundler:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/app
    ports:
      - "3000:3000"  
    environment:
      - NODE_ENV=production
